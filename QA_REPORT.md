# Deck 功能检查记录

日期：2026-09-18。检查基于当前工作区（保留已有未提交修改），本报告记录功能检查时的结果；后续发布版本为 v1.1.3。

## 结果

- `swift test`：63 项 XCTest 测试全部通过，0 失败。
- `bash scripts/build_app.sh`：Release 构建、应用打包与签名校验通过。
- `codesign --verify --deep --strict Deck.app`：通过。
- `git diff --check`：通过。
- 验证环境：Apple Silicon、macOS 27.0（26A428）、Apple Swift 6.4。
- 本地输出：`Deck.app`、`Deck-macOS.zip`。

## 本轮修复与优化

1. 修复 C 事件回调捕获动态 `Self` 导致无法编译的问题；补齐服务状态和配置错误的中英文定义。
2. 映射编辑器限制单字符/空格按键，并阻止大小写、空格别名等重复映射；缺少目标的新增应用映射不能保存。
3. 修复 Command/Control/Option + Space 误触“上一个应用”；保留显式空格映射优先级及按住按键的防重复行为。
4. 显式指定应用标识或路径时，不再按名称模糊匹配其他正在运行的应用。旧的纯名称映射仅做精确名称匹配。
5. 修复应用 Hosts 后配置保存失败时提前丢弃草稿的问题；删除方案也先保存配置，再清除对应草稿。损坏配置在恢复写入前保留备份，存储目录不存在时自动创建。
6. Hosts 悬停预览开关接入方案列表和菜单栏；分组支持折叠及移除文件夹后保留方案。
7. 修复关闭全局分组互斥开关后仍强制互斥的问题；初次使用时示例方案默认停用，保留系统 Hosts 的默认方案。
8. DNS 检查认可同一域名的多个已配置地址，包括 IPv4/IPv6 和域名别名；仍区分匹配、混合结果与不匹配。失效的异步结果不会覆盖当前未应用状态。
9. 启用同一手势的新规则时，自动停用冲突规则，防止新动作被第一条规则遮蔽。详情绑定按稳定 ID 查找，避免列表变化后沿用旧数组索引。
10. 手势识别拒绝非有限坐标/时间戳，避免抬起后重新接触误判为同时轻点；未知动作类型不再悄悄转换为键盘操作。
11. 调整窗口初始/最小尺寸及手势预设按钮换行，编辑弹窗打开时由弹窗处理 Escape。
12. Hosts 应用状态在主窗口重新激活时刷新；测试使用独立配置目录和偏好域，并恢复语言设置。
13. 发布工作流先运行测试；构建脚本增加真正的签名验证；修正 README 中与实现不符的快捷键、语法高亮、搜索位置和 DNS 刷新描述。

## 验证范围

| 模块 | 自动化检查 | 原生界面检查 |
| --- | --- | --- |
| 应用映射 / 快捷键 | 编解码、默认配置、按键/修饰键匹配、重复键、空格后备动作、应用名称回退 | 列表加载、搜索过滤、新增弹窗、无效/重复键禁止保存、Escape 取消 |
| Hosts | 合并、读回核验、保持 inode/权限、只读目录写入、免重复授权、失败不更新方案、草稿重载、损坏配置备份、互斥、分组、DNS 地址比较 | 方案显示、只读查看/刷新、分组折叠、预览帮助、权限状态 |
| 触控板 | 六种手势、释放顺序、移动/长按/额外手指抑制、重复轻点、畸形帧、动作事件序列、配置持久化及冲突规则 | 手势列表、说明动画、属性与动作设置页面、滚动布局 |
| 通用设置 | 中英文文案覆盖、切换回调、主题与位置持久化 | 英文→中文→英文、系统主题→深色→系统主题，菜单即时更新 |
| 启动 / 构建 | Debug 测试、Release 构建、签名完整性 | 新构建启动，各主要页面切换正常，服务状态正常 |

Hosts 写入及授权行为测试使用临时文件和注入的授权函数，没有为测试写入真实 `/etc/hosts`。动作测试截获事件，没有发送测试点击或快捷键。界面测试后已恢复原来的英文、跟随系统主题和分组展开状态。

## 尚未覆盖的环境

- 真实手指触摸、硬件触觉反馈、外接触控板和休眠唤醒后的私有 MultitouchSupport 行为。
- 全局快捷键在其他应用、全屏、多显示器及不同键盘布局下的完整端到端行为。
- 实际管理员授权弹窗、撤销/重新授予辅助功能权限；当前环境已有权限。
- macOS 13–26、Intel Mac、不同 VPN/代理/浏览器的 DNS 缓存行为；DNS 比较使用可控解析结果验证。

以上结果确认已执行检查范围内通过，不能据此保证所有硬件和系统环境下绝对没有 bug。
